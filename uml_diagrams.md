# UML Diagrams — LegacyFinApp-026 (Hello World)

> **Project:** LegacyFinApp-026 · **Language:** Java 25 · **Build:** Maven  
> **Architecture:** Three-tier layered architecture with constructor-based dependency injection  
> **Generated by:** uml-generator agent  
> **Source inputs:** `analysis_results.json`, `ast_analysis.json`, `business_rules_extractor_analysis.json`  
> **Note:** Target directory `/output/` did not exist; file saved to workspace root instead.

---

## Table of Contents

1. [Class Diagram](#1-class-diagram)
2. [Sequence Diagram — Default Greeting Flow (no args)](#2-sequence-diagram--default-greeting-flow-no-args)
3. [Sequence Diagram — Named Recipient Flow (with args)](#3-sequence-diagram--named-recipient-flow-with-args)
4. [Use Case Diagram](#4-use-case-diagram)
5. [Component Diagram](#5-component-diagram)

---

## 1. Class Diagram

Shows all five classes, their fields, methods, visibility modifiers, and the full relationship graph including composition, dependency, and test associations.

```mermaid
classDiagram
    direction TB

    class HelloWorld {
        <<Bootstrap / Entry Point>>
        +main(args: String[]) void$
    }

    class GreetingController {
        <<Presentation Layer>>
        -greetingService: GreetingService
        +GreetingController(greetingService: GreetingService)
        +greet(args: String[]) String
    }

    class GreetingService {
        <<Business Logic Layer>>
        -greetingRepository: GreetingRepository
        +GreetingService(greetingRepository: GreetingRepository)
        +createGreeting(requestedRecipient: String) String
    }

    class GreetingRepository {
        <<Data Access Layer>>
        +getGreetingTemplate() String
        +getDefaultRecipient() String
    }

    class HelloWorldTest {
        <<Test Suite — JUnit 5>>
        +mainPrintsHelloWorldWithTrailingNewline() void
        +controllerUsesFirstArgumentAsRecipient() void
        +serviceFallsBackToDefaultRecipientForBlankInput() void
    }

    %% Composition: Controller owns a GreetingService reference (injected, mandatory)
    GreetingController *-- GreetingService : owns (injected)

    %% Composition: Service owns a GreetingRepository reference (injected, mandatory)
    GreetingService *-- GreetingRepository : owns (injected)

    %% Dependency: HelloWorld creates and wires all three objects (composition root)
    HelloWorld ..> GreetingRepository : creates
    HelloWorld ..> GreetingService : creates
    HelloWorld ..> GreetingController : creates & calls

    %% Test dependencies
    HelloWorldTest ..> HelloWorld : tests (integration)
    HelloWorldTest ..> GreetingController : tests (unit)
    HelloWorldTest ..> GreetingService : tests (unit)
    HelloWorldTest ..> GreetingRepository : uses

    %% Styling — highlight each architectural layer
    style HelloWorld         fill:#4A90D9,stroke:#2C5F8A,color:#fff,font-weight:bold
    style GreetingController fill:#27AE60,stroke:#1A6B3C,color:#fff,font-weight:bold
    style GreetingService    fill:#8E44AD,stroke:#5B2C6F,color:#fff,font-weight:bold
    style GreetingRepository fill:#E67E22,stroke:#9A4F0A,color:#fff,font-weight:bold
    style HelloWorldTest     fill:#7F8C8D,stroke:#505A5B,color:#fff,font-style:italic
```

### Class Summary

| Class | Layer | Responsibility |
|---|---|---|
| `HelloWorld` | Bootstrap | Composition root — wires all layers and triggers execution |
| `GreetingController` | Presentation | Parses CLI `args[]`, extracts recipient, delegates to service |
| `GreetingService` | Business Logic | Resolves recipient (null/blank fallback + trim), formats greeting |
| `GreetingRepository` | Data Access | Provides static config: template `"Hello %s"` and default `"World"` |
| `HelloWorldTest` | Test | JUnit 5 suite — 3 tests covering integration + unit paths |

### Key Relationships

| From | To | Type | Detail |
|---|---|---|---|
| `GreetingController` | `GreetingService` | Composition | `private final` field; null guard in constructor |
| `GreetingService` | `GreetingRepository` | Composition | `private final` field; null guard in constructor |
| `HelloWorld` | `GreetingRepository` | Dependency | Instantiates with `new GreetingRepository()` |
| `HelloWorld` | `GreetingService` | Dependency | Instantiates with `new GreetingService(repository)` |
| `HelloWorld` | `GreetingController` | Dependency | Instantiates and calls `controller.greet(args)` |
| `HelloWorldTest` | `HelloWorld` | Test dependency | End-to-end integration test via `main()` |

---

## 2. Sequence Diagram — Default Greeting Flow (no args)

Traces the complete execution path when the application is run **without** any command-line arguments, resulting in the default greeting `"Hello World"`.

```mermaid
sequenceDiagram
    autonumber

    actor       User   as 👤 User (CLI)
    participant HW     as HelloWorld
    participant Repo   as GreetingRepository
    participant Svc    as GreetingService
    participant Ctrl   as GreetingController
    participant Stdout as System.out

    User ->> HW: java HelloWorld  (no args → args = [])

    Note over HW: === Composition Root: wiring the object graph ===

    HW ->> Repo: new GreetingRepository()
    activate Repo
    Repo -->> HW: repository instance
    deactivate Repo

    HW ->> Svc: new GreetingService(repository)
    activate Svc
    Note right of Svc: Objects.requireNonNull(repository)<br/>✅ repository is non-null
    Svc -->> HW: service instance
    deactivate Svc

    HW ->> Ctrl: new GreetingController(service)
    activate Ctrl
    Note right of Ctrl: Objects.requireNonNull(service)<br/>✅ service is non-null
    Ctrl -->> HW: controller instance
    deactivate Ctrl

    Note over HW: === Business Logic Invocation ===

    HW ->> Ctrl: greet(args=[])
    activate Ctrl

    Note right of Ctrl: args == null || args.length == 0<br/>→ condition TRUE → pass null

    Ctrl ->> Svc: createGreeting(null)
    activate Svc

    Note right of Svc: requestedRecipient == null<br/>→ condition TRUE → use default

    Svc ->> Repo: getDefaultRecipient()
    activate Repo
    Repo -->> Svc: "World"
    deactivate Repo

    Svc ->> Repo: getGreetingTemplate()
    activate Repo
    Repo -->> Svc: "Hello %s"
    deactivate Repo

    Note right of Svc: "Hello %s".formatted("World")<br/>→ "Hello World"

    Svc -->> Ctrl: "Hello World"
    deactivate Svc

    Ctrl -->> HW: "Hello World"
    deactivate Ctrl

    HW ->> Stdout: System.out.println("Hello World")
    Stdout -->> User: Hello World\n
```

### Flow Annotations

| Step | Business Rule | Detail |
|---|---|---|
| `greet(args=[])` | BR-005 | Empty array treated as absent recipient |
| `createGreeting(null)` | BR-008 | Null recipient triggers default-recipient fallback |
| `getDefaultRecipient()` | BR-013 | Returns literal `"World"` |
| `getGreetingTemplate()` | BR-012 | Returns literal `"Hello %s"` |
| `"Hello %s".formatted("World")` | BR-011 | Template substitution produces final greeting |
| `System.out.println` | BR-002 | Output written to stdout with trailing newline |

---

## 3. Sequence Diagram — Named Recipient Flow (with args)

Traces the complete execution path when the application is run **with** `"Alice"` as the first command-line argument, resulting in `"Hello Alice"`.

```mermaid
sequenceDiagram
    autonumber

    actor       User   as 👤 User (CLI)
    participant HW     as HelloWorld
    participant Repo   as GreetingRepository
    participant Svc    as GreetingService
    participant Ctrl   as GreetingController
    participant Stdout as System.out

    User ->> HW: java HelloWorld Alice  (args = ["Alice"])

    Note over HW: === Composition Root: wiring the object graph ===

    HW ->> Repo: new GreetingRepository()
    activate Repo
    Repo -->> HW: repository instance
    deactivate Repo

    HW ->> Svc: new GreetingService(repository)
    activate Svc
    Svc -->> HW: service instance
    deactivate Svc

    HW ->> Ctrl: new GreetingController(service)
    activate Ctrl
    Ctrl -->> HW: controller instance
    deactivate Ctrl

    Note over HW: === Business Logic Invocation ===

    HW ->> Ctrl: greet(args=["Alice"])
    activate Ctrl

    Note right of Ctrl: args != null && args.length > 0<br/>→ condition FALSE → use args[0]

    Ctrl ->> Svc: createGreeting("Alice")
    activate Svc

    Note right of Svc: "Alice" != null && !"Alice".isBlank()<br/>→ condition FALSE → trim recipient

    Note right of Svc: "Alice".trim() → "Alice"<br/>(no leading/trailing whitespace to remove)

    Svc ->> Repo: getGreetingTemplate()
    activate Repo
    Repo -->> Svc: "Hello %s"
    deactivate Repo

    Note right of Svc: "Hello %s".formatted("Alice")<br/>→ "Hello Alice"

    Svc -->> Ctrl: "Hello Alice"
    deactivate Svc

    Ctrl -->> HW: "Hello Alice"
    deactivate Ctrl

    HW ->> Stdout: System.out.println("Hello Alice")
    Stdout -->> User: Hello Alice\n
```

### Comparison: Default vs. Named Recipient

| Decision Point | Default Flow (no args) | Named Recipient Flow |
|---|---|---|
| `GreetingController.greet` | `args == null \|\| args.length == 0` → pass `null` | `args.length > 0` → pass `args[0]` |
| `GreetingService.createGreeting` | `requestedRecipient == null` → call `getDefaultRecipient()` | non-null, non-blank → `trim()` recipient |
| `getDefaultRecipient()` called? | ✅ Yes | ❌ No (skipped) |
| `getGreetingTemplate()` called? | ✅ Yes | ✅ Yes |
| Final output | `"Hello World"` | `"Hello Alice"` |

---

## 4. Use Case Diagram

Shows all actors and their interactions with the **LegacyFinApp-026 Greeting System**, including include/extend relationships between use cases.

```mermaid
graph LR
    %% ── Actors ──────────────────────────────────────────────────────────────
    User(["👤 CLI User"])
    JVM(["⚙️ JVM / OS"])

    %% ── System Boundary ─────────────────────────────────────────────────────
    subgraph GS["🖥️  LegacyFinApp-026 — Greeting System"]
        direction TB

        subgraph Bootstrap["Bootstrap Layer"]
            UC_RUN["Run Application\n(HelloWorld.main)"]
            UC_WIRE["Wire Dependency Graph\n(Composition Root)"]
        end

        subgraph Presentation["Presentation Layer"]
            UC_GREET["Generate Greeting\n(controller.greet)"]
            UC_PARSE["Parse CLI Arguments\n(extract args[0])"]
        end

        subgraph BizLogic["Business Logic Layer"]
            UC_RESOLVE["Resolve Recipient"]
            UC_DEFAULT["Use Default Recipient\n(fallback: 'World')"]
            UC_CUSTOM["Use Custom Recipient\n(trim + apply name)"]
            UC_FORMAT["Format Greeting Message\n(apply template)"]
        end

        subgraph DataAccess["Data Access Layer"]
            UC_TEMPLATE["Provide Greeting Template\n('Hello %s')"]
            UC_DEFNAME["Provide Default Recipient Name\n('World')"]
        end

        subgraph Output["Output"]
            UC_DISPLAY["Display Greeting\n(System.out.println)"]
        end
    end

    %% ── Actor Associations ───────────────────────────────────────────────────
    User     -->|"runs"| UC_RUN
    JVM      -->|"invokes"| UC_RUN

    %% ── Include Relationships ────────────────────────────────────────────────
    UC_RUN    -->|"«include»"| UC_WIRE
    UC_RUN    -->|"«include»"| UC_GREET
    UC_GREET  -->|"«include»"| UC_PARSE
    UC_GREET  -->|"«include»"| UC_RESOLVE
    UC_RESOLVE -->|"«include»"| UC_FORMAT
    UC_FORMAT -->|"«include»"| UC_TEMPLATE
    UC_GREET  -->|"«include»"| UC_DISPLAY

    %% ── Extend Relationships ─────────────────────────────────────────────────
    UC_RESOLVE -.->|"«extend»\n[args null or empty]"| UC_DEFAULT
    UC_RESOLVE -.->|"«extend»\n[args[0] provided]"| UC_CUSTOM
    UC_DEFAULT -->|"«include»"| UC_DEFNAME

    %% ── Styling ──────────────────────────────────────────────────────────────
    style User         fill:#4A90D9,stroke:#2C5F8A,color:#fff,font-weight:bold
    style JVM          fill:#5DADE2,stroke:#2471A3,color:#fff

    style UC_RUN       fill:#4A90D9,stroke:#2C5F8A,color:#fff
    style UC_WIRE      fill:#4A90D9,stroke:#2C5F8A,color:#fff

    style UC_GREET     fill:#27AE60,stroke:#1A6B3C,color:#fff
    style UC_PARSE     fill:#27AE60,stroke:#1A6B3C,color:#fff

    style UC_RESOLVE   fill:#8E44AD,stroke:#5B2C6F,color:#fff
    style UC_DEFAULT   fill:#8E44AD,stroke:#5B2C6F,color:#fff
    style UC_CUSTOM    fill:#8E44AD,stroke:#5B2C6F,color:#fff
    style UC_FORMAT    fill:#8E44AD,stroke:#5B2C6F,color:#fff

    style UC_TEMPLATE  fill:#E67E22,stroke:#9A4F0A,color:#fff
    style UC_DEFNAME   fill:#E67E22,stroke:#9A4F0A,color:#fff

    style UC_DISPLAY   fill:#2C3E50,stroke:#17202A,color:#fff
```

### Actors

| Actor | Type | Description |
|---|---|---|
| **CLI User** | Primary Human Actor | Invokes the application; may optionally supply a recipient name as `args[0]` |
| **JVM / OS** | Primary System Actor | Executes `main()`, passes `args[]`, receives stdout output |

### Use Cases

| Use Case | Layer | Triggered By | Business Rules |
|---|---|---|---|
| Run Application | Bootstrap | CLI User / JVM | BR-001, BR-002 |
| Wire Dependency Graph | Bootstrap | Run Application | BR-003, BR-007 |
| Generate Greeting | Presentation | Run Application | BR-004, BR-005, BR-006 |
| Parse CLI Arguments | Presentation | Generate Greeting | BR-006 |
| Resolve Recipient | Business Logic | Generate Greeting | BR-008, BR-009, BR-010 |
| Use Default Recipient | Business Logic | Resolve Recipient (extend) | BR-008, BR-009, BR-013 |
| Use Custom Recipient | Business Logic | Resolve Recipient (extend) | BR-006, BR-010 |
| Format Greeting Message | Business Logic | Resolve Recipient | BR-011, BR-012, BR-014 |
| Provide Greeting Template | Data Access | Format Greeting Message | BR-012, BR-014 |
| Provide Default Recipient Name | Data Access | Use Default Recipient | BR-013 |
| Display Greeting | Output | Generate Greeting | BR-002 |

---

## 5. Component Diagram

Shows the architectural layers, components within each layer, their dependencies, data flows, and the test harness overlay.

```mermaid
graph TD
    %% ─────────────────────────────────────────────────────────────────────────
    %% EXTERNAL ENTRY POINTS
    %% ─────────────────────────────────────────────────────────────────────────
    CLI["🖥️  CLI\n(java HelloWorld [args])"]
    STDOUT["📤  stdout\n(System.out)"]

    %% ─────────────────────────────────────────────────────────────────────────
    %% LAYER 0 — Bootstrap
    %% ─────────────────────────────────────────────────────────────────────────
    subgraph L0["⚡  Bootstrap Layer"]
        HW["HelloWorld\n────────────────\nComposition Root\nApp Lifecycle Start"]
    end

    %% ─────────────────────────────────────────────────────────────────────────
    %% LAYER 1 — Presentation
    %% ─────────────────────────────────────────────────────────────────────────
    subgraph L1["🎨  Presentation Layer"]
        CTRL["GreetingController\n────────────────\nInput: String[] args\nOutput: String greeting\nNull guard on service"]
    end

    %% ─────────────────────────────────────────────────────────────────────────
    %% LAYER 2 — Business Logic
    %% ─────────────────────────────────────────────────────────────────────────
    subgraph L2["🧠  Business Logic Layer"]
        SVC["GreetingService\n────────────────\nRecipient resolution\nWhitespace trimming\nTemplate formatting\nNull guard on repository"]
    end

    %% ─────────────────────────────────────────────────────────────────────────
    %% LAYER 3 — Data Access
    %% ─────────────────────────────────────────────────────────────────────────
    subgraph L3["🗄️  Data Access Layer"]
        REPO["GreetingRepository\n────────────────\ngetGreetingTemplate()\n→ 'Hello %s'\ngetDefaultRecipient()\n→ 'World'"]
    end

    %% ─────────────────────────────────────────────────────────────────────────
    %% TEST HARNESS
    %% ─────────────────────────────────────────────────────────────────────────
    subgraph LT["🧪  Test Harness (JUnit 5)"]
        TEST["HelloWorldTest\n────────────────\nmainPrintsHelloWorldWithTrailingNewline\ncontrollerUsesFirstArgumentAsRecipient\nserviceFallsBackToDefaultRecipientForBlankInput"]
    end

    %% ─────────────────────────────────────────────────────────────────────────
    %% DATA FLOWS (runtime)
    %% ─────────────────────────────────────────────────────────────────────────
    CLI     -->|"args String[]"| HW
    HW      -->|"new + inject"| CTRL
    HW      -->|"new + inject"| SVC
    HW      -->|"new"| REPO
    HW      -->|"controller.greet(args)"| CTRL
    CTRL    -->|"createGreeting(recipient)"| SVC
    SVC     -->|"getDefaultRecipient()"| REPO
    SVC     -->|"getGreetingTemplate()"| REPO
    REPO    -.->|"'World'"| SVC
    REPO    -.->|"'Hello %s'"| SVC
    SVC     -.->|"'Hello World' / 'Hello Alice'"| CTRL
    CTRL    -.->|"greeting String"| HW
    HW      -->|"println(greeting)"| STDOUT

    %% ─────────────────────────────────────────────────────────────────────────
    %% TEST CONNECTIONS
    %% ─────────────────────────────────────────────────────────────────────────
    TEST    ==>|"integration test"| HW
    TEST    ==>|"unit test"| CTRL
    TEST    ==>|"unit test"| SVC
    TEST    -.->|"uses"| REPO

    %% ─────────────────────────────────────────────────────────────────────────
    %% STYLING
    %% ─────────────────────────────────────────────────────────────────────────
    style CLI     fill:#2C3E50,stroke:#1A252F,color:#fff
    style STDOUT  fill:#2C3E50,stroke:#1A252F,color:#fff

    style HW      fill:#4A90D9,stroke:#2C5F8A,color:#fff,font-weight:bold
    style CTRL    fill:#27AE60,stroke:#1A6B3C,color:#fff,font-weight:bold
    style SVC     fill:#8E44AD,stroke:#5B2C6F,color:#fff,font-weight:bold
    style REPO    fill:#E67E22,stroke:#9A4F0A,color:#fff,font-weight:bold
    style TEST    fill:#7F8C8D,stroke:#505A5B,color:#fff,font-style:italic

    style L0      fill:#EBF5FB,stroke:#4A90D9,stroke-width:2px
    style L1      fill:#EAFAF1,stroke:#27AE60,stroke-width:2px
    style L2      fill:#F5EEF8,stroke:#8E44AD,stroke-width:2px
    style L3      fill:#FEF9E7,stroke:#E67E22,stroke-width:2px
    style LT      fill:#F2F3F4,stroke:#7F8C8D,stroke-width:2px,stroke-dasharray:6 3
```

### Layer Responsibilities

| Layer | Component | Responsibility | Inbound | Outbound |
|---|---|---|---|---|
| **Bootstrap** | `HelloWorld` | Composition root; object graph wiring; lifecycle trigger | CLI `args[]` | Calls `controller.greet(args)` → `stdout` |
| **Presentation** | `GreetingController` | CLI arg parsing; null/empty detection; delegation | `String[] args` | `createGreeting(String)` |
| **Business Logic** | `GreetingService` | Recipient resolution; trim; template formatting | `String requestedRecipient` | `getDefaultRecipient()`, `getGreetingTemplate()` |
| **Data Access** | `GreetingRepository` | Static configuration provider | — | `"Hello %s"`, `"World"` |
| **Test Harness** | `HelloWorldTest` | JUnit 5 validation; stdout capture; assertion | — | Exercises all layers |

### Dependency Direction

```
CLI ──► HelloWorld ──► GreetingController ──► GreetingService ──► GreetingRepository
                              │                      │
                        (presentation)          (business logic)
                              │                      │
                           parses                 resolves
                           args[]               recipient +
                                                formats msg
```

---

## Design Patterns Identified

```mermaid
graph LR
    subgraph Patterns["🏗️  Design Patterns — LegacyFinApp-026"]
        direction LR

        P1["Composition Root\n──────────────\nHelloWorld.main()\nwires the entire\nobject graph"]

        P2["Constructor-Based\nDependency Injection\n──────────────\nAll dependencies passed\nvia constructor params\nwith null guards"]

        P3["Layered Architecture\n(Three-Tier)\n──────────────\nPresentation → Service\n→ Repository"]

        P4["Null Object /\nDefault Value\n──────────────\nGreetingService falls\nback to 'World' when\nrecipient is null/blank"]
    end

    P1 --> P2
    P2 --> P3
    P3 --> P4

    style P1 fill:#4A90D9,stroke:#2C5F8A,color:#fff
    style P2 fill:#27AE60,stroke:#1A6B3C,color:#fff
    style P3 fill:#8E44AD,stroke:#5B2C6F,color:#fff
    style P4 fill:#E67E22,stroke:#9A4F0A,color:#fff
```

---

## Business Rules Coverage Summary

| Rule ID | Description | Covered by Test |
|---|---|---|
| BR-001 | No args → default recipient `"World"` | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-002 | Output written to stdout with newline | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-003 | `GreetingService` must not be null in controller constructor | ❌ Not covered |
| BR-004 | Null `args` → pass `null` to service | ❌ Not covered |
| BR-005 | Empty `args[]` → pass `null` to service | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-006 | Only `args[0]` used; rest discarded | ✅ `controllerUsesFirstArgumentAsRecipient` |
| BR-007 | `GreetingRepository` must not be null in service constructor | ❌ Not covered |
| BR-008 | Null recipient → fallback to default | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-009 | Blank recipient → fallback to default | ✅ `serviceFallsBackToDefaultRecipientForBlankInput` |
| BR-010 | Non-blank recipient must be trimmed | ❌ Not covered |
| BR-011 | Greeting = template formatted with recipient | ✅ `controllerUsesFirstArgumentAsRecipient` |
| BR-012 | Template is `"Hello %s"` | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-013 | Default recipient is `"World"` | ✅ `mainPrintsHelloWorldWithTrailingNewline` |
| BR-014 | Template has exactly one `%s` placeholder | ❌ Not covered |

**Coverage: 9/14 rules (64.3%)** · Uncovered: BR-003, BR-004, BR-007, BR-010, BR-014

---

*Generated by **uml-generator** agent for project LegacyFinApp-026*
