[metadata]
name = hello-world
version = 0.1.0
description = Hello World – Python port of the Java Hello World application

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.10

[options.packages.find]
where = src

[options.extras_require]
test =
    pytest>=7.0
