# Architecture Diagrams — LegacyFinApp-026

> **Project:** LegacyFinApp-026 · **Language:** Java 25 · **Build:** Maven 3.x  
> **Architecture:** Classic 3-Tier Layered + Composition Root Pattern  
> **Generated by:** architecture-analyzer agent  
> **Source inputs:** `analysis_results.json`, `ast_analysis.json`, `code_assessment.json`, `uml_diagrams.md`, `bpmn_diagrams.md`, `documentation_report.md`  
> **Artifact:** `com.example:hello-world:1.0.0`  
> **Note:** Target directory `output/` did not exist and could not be auto-created; file saved to workspace root instead.

---

## Table of Contents

1. [C4 Context Diagram — System and Actors](#1-c4-context-diagram--system-and-actors)
2. [Component Architecture Diagram — Layers and Dependencies](#2-component-architecture-diagram--layers-and-dependencies)
3. [Package Structure Diagram](#3-package-structure-diagram)
4. [Dependency Flow Diagram](#4-dependency-flow-diagram)
5. [Deployment / Runtime Diagram](#5-deployment--runtime-diagram)
6. [Architecture Summary and Recommendations](#6-architecture-summary-and-recommendations)

---

## 1. C4 Context Diagram — System and Actors

Highest-level view of **LegacyFinApp-026** in its operational environment. Shows the system boundary, the primary actor, and all external systems the application interacts with at runtime and build time.

```mermaid
C4Context
    title C4 Level 1 — System Context: LegacyFinApp-026

    Person(operator, "Operator / Developer", "Person who invokes the application from a terminal or CI pipeline. Optionally supplies a recipient name as a CLI argument.")

    System_Boundary(legacyFinApp, "LegacyFinApp-026 System Boundary") {
        System(greetingApp, "LegacyFinApp-026 (hello-world 1.0.0)", "Java 25 console application that generates a personalised greeting and writes it to standard output. Implements a 3-tier layered architecture with constructor-based DI.")
    }

    System_Ext(jvm, "Java Virtual Machine (JDK 25)", "Provides the runtime environment, class loading, garbage collection, and standard library — java.util.Objects, System.out.")
    System_Ext(maven, "Apache Maven 3.x", "Build system. Compiles source, resolves JUnit 5 test dependency, runs Surefire test suite, packages the JAR artifact.")
    System_Ext(junit5, "JUnit Jupiter 5.11.4", "External test framework — test scope only. Provides @Test annotations and assertion APIs consumed by HelloWorldTest. Not present in production runtime classpath.")
    System_Ext(stdout, "Standard Output (stdout)", "Terminal or pipe that receives the formatted greeting string followed by a system newline.")
    System_Ext(cicd, "CI/CD Pipeline (GitHub Actions)", "Automated environment that clones the repository, executes mvn test, and validates build integrity on every push.")

    Rel(operator, greetingApp, "Invokes with optional recipient name", "CLI args[]")
    Rel(greetingApp, stdout, "Writes formatted greeting — e.g. Hello World", "System.out.println()")
    Rel(greetingApp, jvm, "Runs on", "JDK 25 runtime")
    Rel(maven, greetingApp, "Compiles, tests and packages", "mvn package / test")
    Rel(maven, junit5, "Resolves at test time", "Maven Central — test scope")
    Rel(cicd, maven, "Triggers", "mvn test")
    Rel(operator, cicd, "Pushes code to", "git push")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

**Context summary:**

| Actor / System | Role | Notes |
|---|---|---|
| **Operator / Developer** | Only human actor | Provides optional CLI argument; receives stdout output |
| **JVM (JDK 25)** | Runtime host | Provides `java.util.Objects`, `String`, `System.out` |
| **Maven 3.x** | Build orchestrator | Compiles, tests, packages — owns the full lifecycle |
| **JUnit Jupiter 5.11.4** | Test framework | **Test scope only** — absent from production JAR |
| **stdout** | Output channel | The sole output medium — no GUI, no network, no DB |
| **GitHub Actions** | CI/CD runner | Triggers `mvn test`; reports pass/fail per commit |

---

## 2. Component Architecture Diagram — Layers and Dependencies

Detailed view of all **five components** across four architectural layers — their responsibilities, relationships, composition-root wiring, and guard-clause boundaries.

```mermaid
graph TB
    subgraph BOOTSTRAP["🔧  Bootstrap Layer  —  Composition Root"]
        style BOOTSTRAP fill:#1e3a5f,stroke:#4a90d9,stroke-width:2px,color:#ffffff
        HW["HelloWorld\n────────────────────\n+ main(args: String[]) : void\n────────────────────\n• Instantiates GreetingRepository\n• Instantiates GreetingService(repo)\n• Instantiates GreetingController(svc)\n• Calls controller.greet(args)\n• Prints result via System.out.println"]
        style HW fill:#2563eb,stroke:#93c5fd,stroke-width:2px,color:#ffffff
    end

    subgraph PRESENTATION["🖥️  Presentation Layer  —  Input Handling"]
        style PRESENTATION fill:#1a3a2a,stroke:#4ade80,stroke-width:2px,color:#ffffff
        GC["GreetingController\n────────────────────\n- greetingService : GreetingService  [final]\n────────────────────\n+ GreetingController(service)  [guard: requireNonNull]\n+ greet(args: String[]) : String\n────────────────────\n• Parses CLI args[] array\n• Extracts args[0] as requestedRecipient\n• Passes null when args is null or empty\n• Delegates to GreetingService.createGreeting()"]
        style GC fill:#16a34a,stroke:#86efac,stroke-width:2px,color:#ffffff
    end

    subgraph BUSINESS["⚙️  Business Logic Layer  —  Domain"]
        style BUSINESS fill:#3a1a1a,stroke:#f87171,stroke-width:2px,color:#ffffff
        GS["GreetingService\n────────────────────\n- greetingRepository : GreetingRepository  [final]\n────────────────────\n+ GreetingService(repository)  [guard: requireNonNull]\n+ createGreeting(requestedRecipient: String) : String\n────────────────────\n• Resolves effective recipient (null/blank → default)\n• Trims whitespace from valid input\n• Fetches template from repository\n• Returns String.format(template, recipient)"]
        style GS fill:#dc2626,stroke:#fca5a5,stroke-width:2px,color:#ffffff
    end

    subgraph DATA["🗄️  Data Access Layer  —  Configuration"]
        style DATA fill:#2d1b69,stroke:#a78bfa,stroke-width:2px,color:#ffffff
        GR["GreetingRepository\n────────────────────\n+ getGreetingTemplate() : String   → 'Hello %s'\n+ getDefaultRecipient() : String   → 'World'\n────────────────────\n• No external dependencies\n• No constructor injection\n• Stateless — pure configuration provider\n• Hard-coded constants (ISS-OCP)"]
        style GR fill:#7c3aed,stroke:#c4b5fd,stroke-width:2px,color:#ffffff
    end

    subgraph TEST["🧪  Test Layer  —  JUnit Jupiter 5.11.4"]
        style TEST fill:#3a2800,stroke:#fbbf24,stroke-width:2px,color:#ffffff
        HWT["HelloWorldTest\n────────────────────\n+ mainPrintsHelloWorldWithTrailingNewline()     [integration]\n+ controllerUsesFirstArgumentAsRecipient()      [unit]\n+ serviceFallsBackToDefaultRecipientForBlankInput() [unit]\n────────────────────\n• 3 test methods  /  JUnit Jupiter\n• Captures stdout via ByteArrayOutputStream\n• Business rule coverage: 64.3%  (9 / 14 rules)"]
        style HWT fill:#b45309,stroke:#fde68a,stroke-width:2px,color:#ffffff
    end

    %% Runtime wiring — numbered call sequence
    HW -->|"① new GreetingRepository()"| GR
    HW -->|"② new GreetingService(repository)"| GS
    HW -->|"③ new GreetingController(service)"| GC
    HW -->|"④ controller.greet(args)\n    System.out.println(result)"| GC
    GC -->|"⑤ service.createGreeting(requestedRecipient)"| GS
    GS -->|"⑥ repo.getGreetingTemplate()\n    repo.getDefaultRecipient()"| GR

    %% Test associations (dashed)
    HWT -.->|"tests — end-to-end"| HW
    HWT -.->|"tests — unit"| GC
    HWT -.->|"tests — unit"| GS
    HWT -.->|"indirect via service"| GR

    %% External I/O nodes
    ARGS(["🖱️ CLI args[]\n(operator input)"])
    STDOUT(["📤 stdout\nSystem.out.println()"])
    JUNIT(["📦 JUnit Jupiter 5.11.4\n(test scope — not in production JAR)"])
    OBJECTS(["☕ java.util.Objects\nrequireNonNull()\n(JDK standard library)"])

    style ARGS fill:#374151,stroke:#9ca3af,stroke-width:1px,color:#e5e7eb
    style STDOUT fill:#374151,stroke:#9ca3af,stroke-width:1px,color:#e5e7eb
    style JUNIT fill:#374151,stroke:#9ca3af,stroke-width:1px,color:#e5e7eb
    style OBJECTS fill:#374151,stroke:#9ca3af,stroke-width:1px,color:#e5e7eb

    ARGS -->|"passed to main()"| HW
    HW -->|"greeting string"| STDOUT
    JUNIT -.->|"test runtime only"| HWT
    OBJECTS -->|"used by"| GC
    OBJECTS -->|"used by"| GS
```

**Key architectural observations:**

| Concern | Detail |
|---------|--------|
| **Dependency direction** | Strictly top-down: Bootstrap → Controller → Service → Repository. Zero upward references. |
| **Composition root** | `HelloWorld.main()` is the **sole location** where `new` keywords appear for application classes. |
| **Immutability** | All inter-layer references are `final` fields — set once at construction, never reassigned. |
| **Guard clauses** | `Objects.requireNonNull()` in Controller and Service constructors enforces fail-fast at object creation. |
| **No circular dependencies** | The dependency graph is a strict DAG (Directed Acyclic Graph) with no cycles. |
| **Test isolation** | `HelloWorldTest` sits outside the production stack; `junit-jupiter` is absent from the production classpath. |

---

## 3. Package Structure Diagram

Physical source-file layout on disk, Maven standard directory layout, and logical grouping of classes into architectural layers.

```mermaid
graph TD
    ROOT["📁 copilot-test-ktruchcz/\n project root"]
    style ROOT fill:#1e3a5f,stroke:#4a90d9,color:#ffffff

    POM["📄 pom.xml\ncom.example : hello-world : 1.0.0\nJava 25  ·  Maven 3.x"]
    style POM fill:#374151,stroke:#9ca3af,color:#e5e7eb

    SRC["📁 src/"]
    style SRC fill:#374151,stroke:#9ca3af,color:#e5e7eb

    MAIN["📁 main/"]
    MAINJ["📁 java/\n⚠️ default package — no package declaration"]
    style MAIN fill:#374151,stroke:#9ca3af,color:#e5e7eb
    style MAINJ fill:#92400e,stroke:#fcd34d,color:#fef3c7

    TESTDIR["📁 test/"]
    TESTJ["📁 java/\n⚠️ default package — no package declaration"]
    style TESTDIR fill:#374151,stroke:#9ca3af,color:#e5e7eb
    style TESTJ fill:#92400e,stroke:#fcd34d,color:#fef3c7

    HW2["☕ HelloWorld.java\nBootstrap — Composition Root\n13 lines  ·  1 method (main)\nCC: 1.0"]
    style HW2 fill:#2563eb,stroke:#93c5fd,color:#ffffff

    GC2["☕ GreetingController.java\nPresentation Layer — Input Routing\n~15 lines  ·  2 methods\nCC: 1.5  (constructor + greet)"]
    style GC2 fill:#16a34a,stroke:#86efac,color:#ffffff

    GS2["☕ GreetingService.java\nBusiness Logic Layer — Domain\n~20 lines  ·  2 methods\nCC: 2.0  (null+blank branch in createGreeting)"]
    style GS2 fill:#dc2626,stroke:#fca5a5,color:#ffffff

    GR2["☕ GreetingRepository.java\nData Access Layer — Config\n~10 lines  ·  2 methods\nCC: 1.0  (no branching)"]
    style GR2 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff

    HWT2["☕ HelloWorldTest.java\nTest Suite — JUnit Jupiter 5.11.4\n40 lines  ·  3 test methods\nCoverage: 64.3% (9/14 rules)"]
    style HWT2 fill:#b45309,stroke:#fde68a,color:#ffffff

    TARGET["📁 target/  (generated by Maven)"]
    CLASSES["📁 classes/\nHelloWorld.class\nGreetingController.class\nGreetingService.class\nGreetingRepository.class"]
    TESTCLASSES["📁 test-classes/\nHelloWorldTest.class"]
    JAR["📦 hello-world-1.0.0.jar\nmvn package output\nMain-Class: HelloWorld"]
    SUREFIRE["📁 surefire-reports/\nHelloWorldTest.xml\n(3 tests — mvn test)"]

    style TARGET fill:#374151,stroke:#9ca3af,color:#e5e7eb
    style CLASSES fill:#374151,stroke:#9ca3af,color:#e5e7eb
    style TESTCLASSES fill:#374151,stroke:#9ca3af,color:#e5e7eb
    style JAR fill:#374151,stroke:#9ca3af,color:#ffffff
    style SUREFIRE fill:#374151,stroke:#9ca3af,color:#e5e7eb

    ROOT --> POM
    ROOT --> SRC
    ROOT --> TARGET
    SRC --> MAIN
    SRC --> TESTDIR
    MAIN --> MAINJ
    TESTDIR --> TESTJ
    MAINJ --> HW2
    MAINJ --> GC2
    MAINJ --> GS2
    MAINJ --> GR2
    TESTJ --> HWT2
    TARGET --> CLASSES
    TARGET --> TESTCLASSES
    TARGET --> JAR
    TARGET --> SUREFIRE

    WARN["⚠️ ISS-004: All 5 classes reside in the default\n(unnamed) package. Cannot be imported by external\nmodules. Recommended fix: introduce\ncom.example.greeting.* package hierarchy."]
    style WARN fill:#7f1d1d,stroke:#f87171,color:#fca5a5

    MAINJ --> WARN
```

**Package metrics:**

| Metric | Current Value | Target Value |
|--------|--------------|-------------|
| Package depth | 0 (default) | 3 (`com.example.greeting.*`) |
| Classes per package | 4 in default | 1 per layer sub-package |
| Interface count | 0 | 2 (`IGreetingService`, `IGreetingRepository`) |
| Cyclomatic complexity avg | 1.4 ✅ | ≤ 2.0 |
| Total production lines | ~57 | ~80 (with interfaces + Javadoc) |
| Total test lines | ~40 | ~80 (full rule coverage) |

---

## 4. Dependency Flow Diagram

Complete **data and control flow** from operator invocation through to stdout output — including the startup wiring phase, runtime execution phase, recipient resolution decision tree, and message formatting chain.

```mermaid
flowchart TD
    subgraph STARTUP["🔧 Phase 1 — Startup: Object Graph Assembly  (HelloWorld.main)"]
        style STARTUP fill:#1e3a5f,stroke:#4a90d9,color:#ffffff

        S1["① new GreetingRepository()\nno-arg constructor\nstateless configuration object"]
        style S1 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff

        S2["② new GreetingService(repository)\ninjects GreetingRepository"]
        style S2 fill:#dc2626,stroke:#fca5a5,color:#ffffff

        GUARD2{"requireNonNull(repository)\n— guard clause"}
        style GUARD2 fill:#0e7490,stroke:#67e8f9,color:#ffffff

        S3["③ new GreetingController(service)\ninjects GreetingService"]
        style S3 fill:#16a34a,stroke:#86efac,color:#ffffff

        GUARD3{"requireNonNull(service)\n— guard clause"}
        style GUARD3 fill:#0e7490,stroke:#67e8f9,color:#ffffff

        READY["✅ Object graph assembled\nAll dependencies wired\nReady for execution"]
        style READY fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S1 --> S2 --> GUARD2
        GUARD2 -->|"null? → NullPointerException 💥"| NPE1["💥 NullPointerException\n(startup aborts)"]
        style NPE1 fill:#991b1b,stroke:#fca5a5,color:#ffffff
        GUARD2 -->|"not null ✅"| S3 --> GUARD3
        GUARD3 -->|"null? → NullPointerException 💥"| NPE2["💥 NullPointerException\n(startup aborts)"]
        style NPE2 fill:#991b1b,stroke:#fca5a5,color:#ffffff
        GUARD3 -->|"not null ✅"| READY
    end

    subgraph RUNTIME["⚡ Phase 2 — Runtime: Greeting Execution"]
        style RUNTIME fill:#1a2a1a,stroke:#4ade80,color:#ffffff

        R1(["🖱️ String[] args\nreceived from JVM\n(CLI invocation)"])
        style R1 fill:#374151,stroke:#9ca3af,color:#e5e7eb

        R2["controller.greet(args)\n— GreetingController"]
        style R2 fill:#16a34a,stroke:#86efac,color:#ffffff

        R3{"args == null\nor args.length == 0?"}
        style R3 fill:#92400e,stroke:#fcd34d,color:#fef3c7

        R4A["requestedRecipient = args[0]\n(e.g. 'Copilot')"]
        style R4A fill:#16a34a,stroke:#86efac,color:#ffffff

        R4B["requestedRecipient = null\n(trigger default fallback)"]
        style R4B fill:#374151,stroke:#9ca3af,color:#e5e7eb

        R5["service.createGreeting(requestedRecipient)\n— GreetingService"]
        style R5 fill:#dc2626,stroke:#fca5a5,color:#ffffff

        subgraph RESOLUTION["🔍 Recipient Resolution  (GreetingService.createGreeting)"]
            style RESOLUTION fill:#3a1a1a,stroke:#f87171,color:#ffffff

            RES1{"requestedRecipient\n== null  or\n.isBlank() == true?"}
            style RES1 fill:#92400e,stroke:#fcd34d,color:#fef3c7

            RES2["repo.getDefaultRecipient()\nreturns 'World'"]
            style RES2 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff

            RES3["requestedRecipient.trim()\nremoves leading/trailing whitespace"]
            style RES3 fill:#dc2626,stroke:#fca5a5,color:#ffffff

            RES4["effectiveRecipient resolved\ne.g. 'World'  or  'Copilot'"]
            style RES4 fill:#065f46,stroke:#6ee7b7,color:#ffffff

            RES1 -->|"Yes — null or blank"| RES2 --> RES4
            RES1 -->|"No — valid string"| RES3 --> RES4
        end

        subgraph FORMATTING["📝 Message Formatting  (GreetingService + GreetingRepository)"]
            style FORMATTING fill:#2d1b69,stroke:#a78bfa,color:#ffffff

            F1["repo.getGreetingTemplate()\nreturns 'Hello %s'"]
            style F1 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff

            F2["String.format('Hello %s', effectiveRecipient)\nproduces final greeting string"]
            style F2 fill:#dc2626,stroke:#fca5a5,color:#ffffff

            F3["return greeting\ne.g. 'Hello World'\nor 'Hello Copilot'"]
            style F3 fill:#065f46,stroke:#6ee7b7,color:#ffffff

            F1 --> F2 --> F3
        end

        R6["controller.greet() returns String\nback to HelloWorld.main()"]
        style R6 fill:#16a34a,stroke:#86efac,color:#ffffff

        R7(["📤 System.out.println(greeting)\nwrites to stdout with trailing newline\ne.g.  Hello World⏎"])
        style R7 fill:#374151,stroke:#9ca3af,color:#e5e7eb

        R8(["🔚 JVM process exits\nreturn code: 0"])
        style R8 fill:#374151,stroke:#9ca3af,color:#e5e7eb
    end

    %% Phase 1 → Phase 2 handoff
    READY -->|"execution begins"| R1

    %% Runtime flow
    R1 --> R2
    R2 --> R3
    R3 -->|"No args — empty invocation"| R4B
    R3 -->|"Has args — named invocation"| R4A
    R4A --> R5
    R4B --> R5
    R5 --> RES1
    RES4 --> F1
    F3 --> R6
    R6 --> R7 --> R8
```

**Flow analysis:**

| Execution Property | Value |
|---|---|
| **Execution model** | Synchronous, single-threaded, sequential — no async or concurrent operations |
| **Total method calls (happy path)** | 6 (greet → createGreeting → getTemplate → getDefault → format → println) |
| **Decision branches** | 2 conditional gates: args-empty check + recipient null/blank check |
| **Null propagation** | Fully blocked — guard clauses at startup, default fallback at runtime |
| **Side effects** | Single `System.out.println()` write — no file I/O, no network, no DB |
| **Process lifetime** | Instantaneous — exits immediately after `println()` returns |
| **Failure modes** | `NullPointerException` at startup only (injecting null dependencies) |

---

## 5. Deployment / Runtime Diagram

How **LegacyFinApp-026** is built, packaged, deployed, and executed — including Maven build phases, JVM process lifecycle, class loading, and CI/CD integration with GitHub Actions.

```mermaid
graph LR
    subgraph DEV["💻 Developer Workstation / CI Runner"]
        style DEV fill:#1e3a5f,stroke:#4a90d9,color:#ffffff

        CODE["📁 Source Code\nHelloWorld.java\nGreetingController.java\nGreetingService.java\nGreetingRepository.java\npom.xml"]
        style CODE fill:#2563eb,stroke:#93c5fd,color:#ffffff

        TESTCODE["📁 Test Source\nHelloWorldTest.java"]
        style TESTCODE fill:#b45309,stroke:#fde68a,color:#ffffff

        MVNCMD["⌨️ mvn package\nor  mvn test\n(developer or CI runner)"]
        style MVNCMD fill:#374151,stroke:#9ca3af,color:#e5e7eb
    end

    subgraph BUILDPIPELINE["🏗️ Maven Build Pipeline — Lifecycle Phases"]
        style BUILDPIPELINE fill:#1a3a2a,stroke:#4ade80,color:#ffffff

        BP1["validate\n✓ POM syntax check\n✓ project structure"]
        style BP1 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        BP2["compile\nmaven-compiler-plugin 3.13.0\nJava 25 source → JVM bytecode\n→ target/classes/*.class"]
        style BP2 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        BP3["test-compile\nJUnit 5 added to test classpath\n(test scope — NOT in production)\n→ target/test-classes/HelloWorldTest.class"]
        style BP3 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        BP4["test\nmaven-surefire-plugin 3.5.2\nRuns HelloWorldTest  (3 methods)\n✓ PASS  or  ✗ FAIL → build breaks"]
        style BP4 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        BP5["package\nAssemble JAR\nManifest: Main-Class: HelloWorld\n→ target/hello-world-1.0.0.jar"]
        style BP5 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        BP1 --> BP2 --> BP3 --> BP4 --> BP5
    end

    subgraph DEPS["📦 External Dependencies"]
        style DEPS fill:#2d1b69,stroke:#a78bfa,color:#ffffff

        MAVENCENTRAL["🌐 Maven Central\norg.junit.jupiter:junit-jupiter:5.11.4\nScope: TEST ONLY\nNOT included in hello-world-1.0.0.jar"]
        style MAVENCENTRAL fill:#7c3aed,stroke:#c4b5fd,color:#ffffff

        JDK25["☕ JDK 25 Standard Library\njava.util.Objects  — requireNonNull\njava.lang.String  — format, trim, isBlank\njava.lang.System  — out (PrintStream)\njava.io.PrintStream  — println"]
        style JDK25 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff
    end

    subgraph JVMRUNTIME["⚡ JVM 25 Runtime Process"]
        style JVMRUNTIME fill:#3a1a1a,stroke:#f87171,color:#ffffff

        JVMCMD["java -jar hello-world-1.0.0.jar [name]\nor\njava HelloWorld [name]"]
        style JVMCMD fill:#dc2626,stroke:#fca5a5,color:#ffffff

        subgraph CLASSLOAD["🔃 JVM Classloader — On-Demand Class Loading"]
            style CLASSLOAD fill:#7f1d1d,stroke:#f87171,color:#ffffff
            CL1["HelloWorld.class  — loaded first  (Main-Class entry)"]
            CL2["GreetingRepository.class  — loaded at new GreetingRepository()"]
            CL3["GreetingService.class  — loaded at new GreetingService(repo)"]
            CL4["GreetingController.class  — loaded at new GreetingController(svc)"]
            style CL1 fill:#991b1b,stroke:#fca5a5,color:#ffffff
            style CL2 fill:#991b1b,stroke:#fca5a5,color:#ffffff
            style CL3 fill:#991b1b,stroke:#fca5a5,color:#ffffff
            style CL4 fill:#991b1b,stroke:#fca5a5,color:#ffffff
        end

        subgraph EXECSTACK["📞 Call Stack — Single Main Thread"]
            style EXECSTACK fill:#7f1d1d,stroke:#f87171,color:#ffffff
            E1["HelloWorld.main(args)\n— Composition Root — FRAME 1"]
            E2["GreetingController.greet(args)\n— FRAME 2"]
            E3["GreetingService.createGreeting(recipient)\n— FRAME 3"]
            E4["GreetingRepository.getGreetingTemplate()\nGreetingRepository.getDefaultRecipient()\n— FRAME 4"]
            style E1 fill:#2563eb,stroke:#93c5fd,color:#ffffff
            style E2 fill:#16a34a,stroke:#86efac,color:#ffffff
            style E3 fill:#dc2626,stroke:#fca5a5,color:#ffffff
            style E4 fill:#7c3aed,stroke:#c4b5fd,color:#ffffff
            E1 --> E2 --> E3 --> E4
        end

        JVMCMD --> CLASSLOAD
        JVMCMD --> EXECSTACK
    end

    subgraph CICD["🔄 CI/CD — GitHub Actions"]
        style CICD fill:#3a2800,stroke:#fbbf24,color:#ffffff

        GITPUSH["git push  (main branch)\nDeveloper triggers CI"]
        style GITPUSH fill:#b45309,stroke:#fde68a,color:#ffffff

        GHRUNNER["GitHub Actions Runner\n① actions/checkout\n② actions/setup-java  (JDK 25)\n③ mvn test\n④ Report: PASS / FAIL badge"]
        style GHRUNNER fill:#b45309,stroke:#fde68a,color:#ffffff

        GITPUSH --> GHRUNNER
    end

    subgraph OUTPUT["📤 Outputs"]
        style OUTPUT fill:#374151,stroke:#9ca3af,color:#e5e7eb

        JAROUT["📦 hello-world-1.0.0.jar\ntarget/ directory\nSelf-contained\nNo runtime dependencies"]
        style JAROUT fill:#374151,stroke:#9ca3af,color:#ffffff

        STDOUTOUT["🖥️ Terminal — stdout\nHello World\nor\nHello [name]"]
        style STDOUTOUT fill:#374151,stroke:#9ca3af,color:#ffffff

        TESTREPORT["📋 Surefire Report\ntarget/surefire-reports/\nHelloWorldTest.xml\n3 tests — PASS"]
        style TESTREPORT fill:#374151,stroke:#9ca3af,color:#ffffff
    end

    CODE --> MVNCMD
    TESTCODE --> MVNCMD
    MVNCMD --> BUILDPIPELINE
    MAVENCENTRAL -.->|"test scope — compile only"| BP3
    JDK25 -->|"compile + runtime classpath"| BP2
    BP4 --> TESTREPORT
    BP5 --> JAROUT
    JAROUT -->|"java -jar ..."| JVMRUNTIME
    JVMRUNTIME --> STDOUTOUT
    GHRUNNER -->|"delegates to"| BUILDPIPELINE
```

**Deployment characteristics:**

| Property | Value |
|----------|-------|
| **Artifact type** | Executable JAR (`hello-world-1.0.0.jar`) |
| **Runtime dependencies** | **Zero** — pure JDK 25, no third-party JARs needed at runtime |
| **Process type** | Short-lived CLI process — exits after single `println()` |
| **Memory footprint** | Minimal — 4 tiny class instances, no heap-intensive data structures |
| **Thread model** | Single main thread — no concurrency |
| **Startup time** | < 100 ms (JVM warm-up + trivial object creation) |
| **JUnit 5 in production JAR** | ❌ Excluded — test scope prevents inclusion |
| **CI gate** | `mvn test` must pass; 3/3 tests required green before merge |
| **Execution environments** | Any JDK 25-compatible JVM (Windows / Linux / macOS / Docker) |

---

## 6. Architecture Summary and Recommendations

### 6.1 Architecture Quality Scorecard

```mermaid
xychart-beta
    title "LegacyFinApp-026 — Architecture Quality Dimensions (score out of 10)"
    x-axis ["Separation\nof Concerns", "Single\nResponsibility", "Dependency\nDirection", "Guard\nClauses", "Immutability", "Interface\nAbstractions", "Package\nOrganisation", "Extensibility", "Test\nCoverage", "Build\nHygiene"]
    y-axis "Score" 0 --> 10
    bar [9, 9, 8, 9, 9, 1, 2, 4, 6, 9]
    line [9, 9, 8, 9, 9, 1, 2, 4, 6, 9]
```

**Overall architecture score: 7.2 / 10** — Strong structural foundation, low complexity. Primary gaps in interface abstractions and package organisation.

---

### 6.2 Architectural Strengths

```mermaid
graph LR
    subgraph STRENGTHS["✅ Confirmed Architectural Strengths"]
        style STRENGTHS fill:#1a3a2a,stroke:#4ade80,color:#ffffff

        S1["🏗️ Clean 3-Tier Layering\nZero cross-layer coupling violations\nBootstrap → Presentation → Business → Data"]
        style S1 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S2["💉 Constructor Injection\nAll dependencies via constructor\nFinal fields — immutable after construction\nNo field injection / no setters"]
        style S2 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S3["🎯 Single Composition Root\nHelloWorld.main() is the ONLY wiring point\nnew keyword used in exactly one place"]
        style S3 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S4["🛡️ Fail-Fast Guard Clauses\nObjects.requireNonNull() at all boundaries\nNull dependencies caught at startup\nnot silently at runtime"]
        style S4 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S5["🔁 Null Object / Default Fallback\nDefault recipient World prevents null propagation\nNo NullPointerException in normal flow"]
        style S5 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        S6["📦 Reproducible Maven Build\nAll plugin versions pinned explicitly\nUTF-8 encoding enforced\nmvn package produces identical JAR every time"]
        style S6 fill:#065f46,stroke:#6ee7b7,color:#ffffff
    end
```

---

### 6.3 Identified Issues and Recommendations

```mermaid
graph TD
    subgraph ISSUES["⚠️ Architectural Issues — Prioritised"]
        style ISSUES fill:#1c1c1c,stroke:#9ca3af,color:#ffffff

        ISS1["🔴 HIGH — ISS-004\nNo Package Declarations\nAll 5 classes in unnamed default package\nImpact: Cannot be imported by external code\nFix: com.example.greeting  +  layer sub-packages"]
        style ISS1 fill:#991b1b,stroke:#fca5a5,color:#ffffff

        ISS2["🔴 HIGH — DIP Violation\nNo Interface Abstractions\nController couples to GreetingService concrete class\nService couples to GreetingRepository concrete class\nFix: Extract IGreetingService + IGreetingRepository\nHigh-level modules depend on abstractions not concretions"]
        style ISS2 fill:#991b1b,stroke:#fca5a5,color:#ffffff

        ISS3["🟡 MEDIUM — OCP Violation\nHard-coded Configuration in GreetingRepository\nHello %s and World are literal constants\nChange requires recompilation  (no extension point)\nFix: Constructor-inject template + default recipient\nor load from application.properties"]
        style ISS3 fill:#92400e,stroke:#fcd34d,color:#fef3c7

        ISS4["🟡 MEDIUM — Test Coverage 64.3%\n5 of 14 business rules not covered by tests\nMissing: null args  ·  multi-arg  ·  empty-string\n         whitespace-only name  ·  template format\nFix: Add 5 targeted @Test methods to HelloWorldTest"]
        style ISS4 fill:#92400e,stroke:#fcd34d,color:#fef3c7

        ISS5["🟢 LOW — No Javadoc\n0 of 5 classes have Javadoc comments\nFix: Add @param @return @throws\nto all public methods — 30 min effort"]
        style ISS5 fill:#065f46,stroke:#6ee7b7,color:#ffffff

        ISS6["🟢 LOW — No Logging Framework\nSystem.out.println used directly in HelloWorld\nCannot control log level or format\nFix: Introduce SLF4J facade + Logback backend"]
        style ISS6 fill:#065f46,stroke:#6ee7b7,color:#ffffff
    end
```

---

### 6.4 Target Architecture (Recommended Evolution)

The diagram below shows the recommended future state after applying all high-priority fixes — interface extraction, package hierarchy, and configuration externalisation.

```mermaid
graph TB
    subgraph FUTURE["🚀 Target Architecture — After Remediation"]
        style FUTURE fill:#1e3a5f,stroke:#4a90d9,color:#ffffff

        subgraph PKG_BOOT["📦 com.example.greeting  (Bootstrap)"]
            style PKG_BOOT fill:#1d4ed8,stroke:#93c5fd,color:#ffffff
            FHW["HelloWorld\nComposition Root — unchanged\nWires concrete impls to interfaces"]
            style FHW fill:#2563eb,stroke:#93c5fd,color:#ffffff
        end

        subgraph PKG_API["📦 com.example.greeting.api  (Interfaces — NEW)"]
            style PKG_API fill:#0e4f6e,stroke:#67e8f9,color:#ffffff
            IGS["«interface» IGreetingService\n+ createGreeting(String) : String"]
            IGR["«interface» IGreetingRepository\n+ getGreetingTemplate() : String\n+ getDefaultRecipient() : String"]
            style IGS fill:#0891b2,stroke:#67e8f9,color:#ffffff
            style IGR fill:#0891b2,stroke:#67e8f9,color:#ffffff
        end

        subgraph PKG_CTRL["📦 com.example.greeting.controller  (Presentation)"]
            style PKG_CTRL fill:#1a3a2a,stroke:#4ade80,color:#ffffff
            FGC["GreetingController\n— depends on IGreetingService (interface)\n— NOT on GreetingService (concrete)"]
            style FGC fill:#16a34a,stroke:#86efac,color:#ffffff
        end

        subgraph PKG_SVC["📦 com.example.greeting.service  (Business Logic)"]
            style PKG_SVC fill:#3a1a1a,stroke:#f87171,color:#ffffff
            FGS["GreetingService  implements IGreetingService\n— depends on IGreetingRepository (interface)\n— NOT on GreetingRepository (concrete)"]
            style FGS fill:#dc2626,stroke:#fca5a5,color:#ffffff
        end

        subgraph PKG_REPO["📦 com.example.greeting.repository  (Data Access)"]
            style PKG_REPO fill:#2d1b69,stroke:#a78bfa,color:#ffffff
            FGR["GreetingRepository  implements IGreetingRepository\n— template injected via constructor\n— defaultRecipient injected via constructor\n— OR loaded from application.properties"]
            style FGR fill:#7c3aed,stroke:#c4b5fd,color:#ffffff
        end

        FHW -->|"wires concrete → interface"| FGC
        FHW -->|"wires concrete → interface"| FGS
        FHW -->|"wires concrete → interface"| FGR
        FGC -->|"depends on"| IGS
        FGS -->|"implements"| IGS
        FGS -->|"depends on"| IGR
        FGR -->|"implements"| IGR
    end
```

---

### 6.5 Migration Roadmap

| Priority | Category | Change | Benefit | Effort |
|----------|----------|--------|---------|--------|
| 🔴 **1 — High** | Package Structure | Add `com.example.greeting` base package + `controller`, `service`, `repository`, `api` sub-packages | External imports enabled; IDE navigation improved; modular architecture enforced | **Low** (rename + move) |
| 🔴 **2 — High** | Interface Abstractions | Extract `IGreetingService` and `IGreetingRepository` interfaces in `api` package | DIP compliance; mockable in tests (no Mockito hacks needed); swappable implementations | **Low–Medium** (define 2 interfaces, update 3 `import`s) |
| 🟡 **3 — Medium** | Config Externalisation | Move `"Hello %s"` and `"World"` to constructor parameters or `application.properties` | OCP compliance — behaviour changes without recompilation | **Medium** (add config loading) |
| 🟡 **4 — Medium** | Test Coverage | Add 5 missing test cases: null args, empty-string, whitespace-name, multi-arg ignored, template format | Coverage: 64.3% → ~100% of business rules | **Low** (5 test methods) |
| 🟢 **5 — Low** | Javadoc | Add `@param`, `@return`, `@throws` to all public methods | IDE Javadoc tooltips; API documentation generation via `mvn javadoc:javadoc` | **Low** (~30 min) |
| 🟢 **6 — Low** | Logging | Replace `System.out.println` with SLF4J + Logback | Structured logs; log-level control; CI-friendly; production-grade observability | **Low** (add 1 dependency + config) |

---

> **Diagram format:** All 9 diagrams in this document use **Mermaid** syntax exclusively.  
> No PlantUML syntax. No ASCII art.  
>
> **Diagram legend:**
> - 🔵 **Blue** — Bootstrap / Composition Root layer  
> - 🟢 **Green** — Presentation / Controller layer  
> - 🔴 **Red** — Business Logic / Service layer  
> - 🟣 **Purple** — Data Access / Repository layer  
> - 🟠 **Amber** — Test layer (JUnit 5)  
> - ⚫ **Grey** — External systems, runtime environment, I/O  
> - 🩵 **Cyan** — Interfaces / Abstractions (target architecture)  
>
> **Total diagrams generated:** 9  
> C4 Context · Component Architecture · Package Structure · Dependency Flow ·  
> Deployment/Runtime · Quality Scorecard · Strengths · Issues · Target Architecture
